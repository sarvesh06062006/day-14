#include <stdio.h>

int main() {
    int w, x, y;
    scanf("%d", &w);
    scanf("%d", &x);
    scanf("%d", &y);
    int fixed_cost = 100;
    int profit = (w * (x - y)) - fixed_cost;
    printf("%d\n", profit);
    
    return 0;
}
